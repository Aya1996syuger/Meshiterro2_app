<span> Bookers </span>
<%= flash[:notice] %>
 <%= render 'users/list', book: @book, user: @user %>

 <h2>Books</h2>
  <table>
   <thead>
    <tr>
     <th>Title</th>
     <th>Opinions</th>
     <th colspan="3"></th>
    </tr>
   </thead>
    <tbody>
     <tr>
       <% @books.each do |book| %>
        <td><%= image_tag('no_image.jpg') %></td>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
       <% end %>
      </tr>
  </table>
 </div>


